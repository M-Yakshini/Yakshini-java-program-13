class Solution {
    public int[] getModifiedArray(int n,int[][] u){
        int[] a=new int[n];
        for(int[] x:u){
            a[x[0]]+=x[2];
            if(x[1]+1<n) a[x[1]+1]-=x[2];
        }
        for(int i=1;i<n;i++) a[i]+=a[i-1];
        return a;
    }
}
