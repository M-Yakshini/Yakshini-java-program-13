class Solution {
    public int maxRotateFunction(int[] a){
        int sum=0,f=0,n=a.length;
        for(int i=0;i<n;i++){sum+=a[i];f+=i*a[i];}
        int r=f;
        for(int i=n-1;i>0;i--){
            f+=sum-n*a[i];
            r=Math.max(r,f);
        }
        return r;
    }
}
