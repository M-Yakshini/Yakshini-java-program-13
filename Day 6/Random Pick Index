class Solution {
    int[] a;
    Random r=new Random();
    public Solution(int[] nums){a=nums;}
    public int pick(int t){
        int c=0,res=0;
        for(int i=0;i<a.length;i++)
            if(a[i]==t&&r.nextInt(++c)==0) res=i;
        return res;
    }
}
