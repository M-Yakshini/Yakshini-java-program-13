class Solution {
    public int[][] reconstructQueue(int[][] p){
        Arrays.sort(p,(a,b)->a[0]==b[0]?a[1]-b[1]:b[0]-a[0]);
        List<int[]> l=new ArrayList<>();
        for(int[] x:p) l.add(x[1],x);
        return l.toArray(new int[p.length][]);
    }
}
